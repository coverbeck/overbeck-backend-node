{% extends "layouts/base.njk" %}

{% block title %}Loch Lomond Reservoir{% endblock %}

{% block content %}
<h1>Loch Lomond Reservoir Level</h1>
<div style="max-width: 700px">
  <canvas id="lochlomond-chart"></canvas>
</div>
<p>
  Data screen scraped from
  <a href="https://www.cityofsantacruz.com/government/city-departments/water/weekly-water-conditions">here</a>.
</p>
<p>Last checked: {{ lastChecked }}</p>
{% endblock %}

{% block head %}
<script src="https://cdn.jsdelivr.net/npm/chart.js@4"></script>
<script src="https://cdn.jsdelivr.net/npm/chartjs-adapter-date-fns@3/dist/chartjs-adapter-date-fns.bundle.min.js"></script>
{% endblock %}

{% block scripts %}
<script>
  const labels = {{ labels | dump | safe }};
  const values = {{ values | dump | safe }};

  new Chart(document.getElementById('lochlomond-chart'), {
    type: 'line',
    data: {
      datasets: [{
        label: 'Reservoir Level (%)',
        data: labels.map((date, i) => ({ x: date, y: values[i] })),
        fill: true,
      }],
    },
    options: {
      scales: {
        x: {
          type: 'time',
          time: { unit: 'month' },
        },
      },
    },
  });
</script>
{% endblock %}
